
// * open 	Create all components on a single page
// * Add form element to HTML to accept author, quote (do rating later), 
//   and rating; be sure to prevent normal form submission process
// * Create constructor object so that each quote is in its own object 
// * Add list element to HTML (pull data from objects)
// * Create buttons (event handlers) as follows:
//   1) Add quote
//   2) Submit quote
//   3) Delete quote
//   4) Generate random quote  (use "lowdash" or "underscore" and then select) 
// * Create div elements
//   1) Div wrapper (display) that captures authors, quotes, and ratings
//   2) Div wraper for quotes
//   3) Div wrapper for authors
//   4) Div wrapper for ratings
//   5) Div for each new individual quote
//   6) Div for each new individual author
//   7) Div for each new individual rating
// * Create div element for quotes, which is "appended" after each submission
// * Create element for quote rating (make rating 1 through 5)
// * Be able to sort on author or quote (create object for each quote
//   that has author, quote, and rating); will then be able to look at all quotes 
//   by author
// * Manipulate the DOM each time a quote / author is added (submited) or
//   removed (deleted); use jQuery . . . append, etc.













$(document).on('ready', function() {
  
});